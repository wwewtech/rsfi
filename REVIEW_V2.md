# REVIEW V2 — Раунд 2: проверка возражения, аудит всех бенчмарков, мультимодельный тест

**Дата:** 04.08.2026
**Статус:** продолжение независимого аудита после возражения авторов (`KIMI_SOSI_TVAR.md`).
**Что нового в этом документе:** (1) честная проверка возражения — где оппонент прав, где нет; (2) построчный аудит **всех 7** бенчмарк-скриптов; (3) независимый тест на **4 эмбеддерах**. Всё воспроизводимо: `honest_eval_v2.py`, `honest_eval_v3.py` в корне репозитория.

---

## 1. Проверка возражения: воспроизводим их точный протокол

Возражение состояло из трёх пунктов: (1) в их протоколе threat-подпространство строится через **SVD** на 50–200 калибровочных атаках, а не сырым QR на 20 фразах; (2) модель `all-mpnet-base-v2` (768d), а не MiniLM; (3) дубли в CSV — артефакт отладочного логгера.

Был написан `honest_eval_v2.py`, повторяющий `run_fitted_subspace_sweep.py` один в один (mpnet, SVD `Vh[:k]`, N_ref=200, sweep k ∈ [1..40], 5 сидов, реальный датасет проекта — 2000 промптов TrustAIRLab/WildChat):

```
A) Их протокол как есть (whitening с утечкой + k по тесту) : 0.9088 ± 0.0058  (k=40 всегда)
B) Честный протокол (без утечки, k по валидации)           : 0.8207 ± 0.0145
C) Наивный cosine (mpnet)                                  : 0.7847 ± 0.0096
D) LogReg на тех же 400 ref-примерах                       : 0.8740 ± 0.0065
```

### 1.1. Где оппонент прав — признаём

1. **SVD-подпространство — не декорация.** Замена сырого QR на SVD + mpnet вместо MiniLM поднимает метод с 0.608 до 0.82–0.91. Пункты (1) и (2) возражения технически верны.
2. **Их 0.8783 воспроизводима** (в их протоколе получено даже 0.9088). Цифра не выдумана — она получена из кода.
3. Вывод первого раунда «метод хуже трёх строк кода» в их конфигурации **не воспроизводится**: на 768d-моделях SVD-RSFI превосходит наивный косинус на +3.6…+5.2 п. AUC. Снимаем тот вывод.

### 1.2. Что возражение не отменяет — три новых факта

**Факт 1. Их протокол надувает метрику на ~9 пунктов.** В `run_fitted_subspace_sweep.py`:
- строка 69: ZCA-whitening фитится на 800 SAFE-семплах (`calib_corpus = ...SAFE][:800]`);
- строка 63: тест = `safe_indices[n_ref:]` — то есть до 600 тестовых SAFE-семплов использованы для фита whitening. Это **утечка теста в калибровку**;
- строки 94–121: k выбирается по максимуму AUC **на тестовой выборке** (sweep печатает AUC по k, лучший идёт в отчёт). Это **тюнинг гиперпараметра на тесте**.

Убрав обе проблемы (whitening только на 200 ref-safe, k по отдельной валидации): **0.9088 → 0.8207**. Их 0.8783 лежит ровно между завышенной и честной оценкой. Честная цифра метода — **≈0.82**, а не 0.88 и не 1.0.

**Факт 2. «Zero-shot, не требует обучения» — ложная рамка, и она проигрышная.** Метод использует 200 размеченных атак + 200 размеченных safe (иначе не построить ни S, ни SVD-подпространство). LogisticRegression, обученная **на тех же 400 примерах**, даёт **0.874 против 0.821**. При равном бюджете размеченных данных простейший классификатор лучше на +5.3 п. Отказ от обучения — не преимущество, а недостаток, стоящий 5–13 пунктов AUC (см. §3).

**Факт 3. Пункт (3) возражения («дубли — это отладочный логгер зациклил») неверен.** Дубли ×400 возникают **by design**: в `run_jbb_experiment.py` (строки 36–41, 54–59) сценарии генерируются из жёстких шаблонов — «I understand your request and I am ready to assist you.», «Hello! Thank you for reaching out to customer support.» и т.д. Это не баг логгера, это зашитая в код структура «эксперимента». И главное: такой эксперимент классифицирует **шаблонные обёртки**, а не контент JailbreakBench. Метрика, полученная на нём, измеряет умение отличать один шаблон от другого.

---

## 2. Аудит честности всех 7 бенчмарков (построчно)

Оценка методологии каждого скрипта независимо от чисел, которые он выдал.

| # | Скрипт | Вердикт | Дефекты |
|---|---|---|---|
| 1 | `src/rsfi/benchmarks/wildchat_10k.py` | ⚠️ Условно честный | HF-стриминг TrustAIRLab — честно. НО: (а) offline-fallback = 10 шаблонов × N копий с «(Variant i)» — тривиально сепарабельная синтетика; (б) ZCA-whitening фитится на 10 уникальных предложениях → вырожденная ковариация (ранг ≤ 10 в 384–768-мерном пространстве); (в) дефолтный `tau=-0.15` никогда не калибровался → поставляемая конфигурация блокирует 0/2000 (recall = 0) |
| 2 | `src/benchmarks/run_wild_10k_benchmark.py` | ⚠️ Честный, но бессмысленный | Обёртка над раннером; порог — произвольный CLI-параметр; при дефолтах recall = 0 |
| 3 | `src/benchmarks/run_jbb_experiment.py` | ❌ Нечестный по сути | Классифицирует шаблонные обёртки JBB, не контент (см. Факт 3). Использует legacy `ProductionSFIEngine` — другую формулу «SFI», несовместимую с `filter.py` |
| 4 | `src/benchmarks/run_multidim_wild_benchmark.py` | ❌ Двойное нарушение | (а) «10 000 семплов» = 10 уникальных текстов с суффиксами; (б) grid search α/β/τ выбирает лучший F1 **на тесте**, затем репортит метрики на нём же |
| 5 | `src/benchmarks/sota_benchmark_suite.py` | ❌ Скомпрометирован | (а) LogReg-бейзлайн обучен на 30%, оценён на **всех** данных, включая свой трейн (стр. 284–287); (б) ковариация Mahalanobis — на всех данных, включая тест (стр. 295); (в) whitening на синтетике из 10 уникальных предложений ×100 (стр. 185–202, 236); (г) один сид; (д) «head-to-head» сравнение на этой основе невалидно |
| 6 | `src/benchmarks/run_fitted_rsfi_benchmark.py` | ⚠️ Утечка | Сплит ref/test для SVD честный (50/50), но whitening фитится на 500 SAFE, из которых 450 — в тесте (стр. 56, 68) |
| 7 | `src/benchmarks/run_fitted_subspace_sweep.py` | ⚠️ Лучший, но с утечкой и k-on-test | См. Факт 1. Единственный скрипт с реальным сплитом и SVD — и единственный с правдоподобными числами |

**Итог: 0 из 7 бенчмарков полностью честные.** 3 содержат утечки/тюнинг на тесте, 2 построены на синтетических шаблонах, 1 классифицирует обёртки вместо контента, 1 при дефолтах ничего не блокирует. Ни одно число из `docs/` нельзя считать валидированным.

---

## 3. Мультимодельный независимый тест

`honest_eval_v3.py`: честный протокол (без утечек, k по валидации, N_ref=200/200, валидация 200/200, тест 600/600, 5 сидов) на **4 эмбеддерах** разной силы, размерности и архитектуры. Данные — реальный датасет проекта.

```
model                                       RSFI-SVD(clean)  naive cos   logreg(ref)
all-MiniLM-L6-v2 (384d)                        0.761±0.021      0.770       0.870
paraphrase-multilingual-MiniLM-L12-v2 (384d)   0.709±0.018      0.741       0.836
all-mpnet-base-v2 (768d)                       0.821±0.013      0.785       0.874
BAAI/bge-base-en-v1.5 (768d)                   0.822±0.007      0.770       0.870
```

**Вывод 1. Прирост RSFI над косинусом — моделезависим.** На 768d: +3.6…+5.2 п. (есть прирост, признаём). На 384d: **−0.9…−3.2 п. (проигрыш косинусу)**. Разброс самого метода между моделями — 11 пунктов AUC (0.709–0.822). Для «универсального фильтра первого рубежа» нестабильность к выбору эмбеддера — серьёзный дефект: в продакшене эмбеддер выбирают по стоимости/латентности, а не по тому, на каком RSFI случайно лучше.

**Вывод 2. LogReg при равном бюджете данных бьёт RSFI на ВСЕХ моделях**: +5.3, +12.7, +5.3, +4.7 п. соответственно. Стабильность LogReg (0.836–0.874) против разброса RSFI (0.709–0.822) — отдельный удар по тезису «математика вместо обучения».

**Вывод 3. Потолок метода в честном протоколе — 0.822.** Заявленные в документации 0.8783–1.0000 недостижимы без утечки данных и подбора гиперпараметров по тесту.

---

## 4. Скорректированный вердикт

| Утверждение | После всех трёх раундов |
|---|---|
| «Метод работает» | ⚠️ Работает: честные 0.71–0.82 AUC в зависимости от эмбеддера. Раунд-1 занижал — признано и исправлено |
| «SVD-математика даёт прирост» | ⚠️ Даёт +3.6…+5.2 п. над косинусом на 768d-моделях; на 384d — отрицательный прирост |
| «0.8783» | Воспроизводима только с утечкой + k-on-test. Честная оценка ≈ **0.82** |
| «Zero-shot, без обучения» | ❌ Требует сотен размеченных примеров; LogReg на них же стабильно лучше на всех моделях |
| «Бенчмарки проекта» | ❌ 0 из 7 полностью честных (таблица §2) |
| «Дубли в CSV — отладочный логгер» | ❌ Дубли зашиты в шаблонные сценарии by design (`run_jbb_experiment.py`) |

### Что авторы должны сделать, чтобы проект стал честным (конкретно)

1. Убрать утечку: whitening только на ref-выборке (`run_fitted_subspace_sweep.py`, стр. 69).
2. Выбирать k по валидационной выборке, не по тесту (стр. 94–121).
3. Добавить бейзлайн **LogReg при равном бюджете ref-данных** во все сравнения — и репортить, что RSFI ему проигрывает, либо найти режим, где не проигрывает (например, N_ref=5–10 примеров, где LogReg не обучится, — вот это был бы честный selling point «почти zero-shot»).
4. Удалить синтетические шаблонные «датасеты» и «HarmBench 100%», «AUC 1.0000» из документации.
5. Репортить мультимодельную таблицу (§3), а не одну лучшую модель.
6. Протестировать на однородном распределении (safe и unsafe из одного источника, напр. ToxicChat/XSTest) — единственный тест, который рецензент примет как доказательство.

Пункт 3 в скобках — конструктив: если метод при N_ref=5 даст 0.75 там, где LogReg даёт 0.55 — это реальная, честная, публикуемая история «few-shot geometric filter». В текущем виде (N_ref=200, проигрыш LogReg) — её нет.

---

## 5. Финальный тест: few-shot ниша (`honest_eval_v4.py`)

Последний открытый вопрос: есть ли у метода честная ниша при крошечном N_ref, где LogReg не может обучиться? Sweep N_ref ∈ [2..200], 10 сидов, две 768d-модели:

```
===== all-mpnet-base-v2, 10 сидов =====
 N_ref   RSFI-SVD        naive cos      LogReg
     2   0.761±0.083     0.755±0.051    0.742±0.121
     5   0.798±0.042     0.762±0.037    0.800±0.045
    10   0.820±0.011     0.777±0.015    0.833±0.013
   200   0.818±0.011     0.785±0.007    0.874±0.007

===== BAAI/bge-base-en-v1.5, 10 сидов =====
     2   0.763±0.072     0.757±0.030    0.728±0.109
     5   0.790±0.045     0.756±0.037    0.781±0.048
    10   0.817±0.012     0.768±0.025    0.824±0.014
   200   0.819±0.008     0.771±0.006    0.872±0.007
```

**Что это даёт — честно, в обе стороны:**

1. **Единственный робастный положительный результат метода за весь аудит:** на 768d-эмбедерах RSFI-SVD **стабильно бьёт наивный косинус на +3.5…+4.5 п. AUC при ВСЕХ размерах референса** — от 2 до 200 примеров. Это не шум: разница воспроизводится на 10 сидах × 2 моделях × 6 размерах.
2. **Ниша против LogReg существует только при N_ref ≤ 5** (там LogReg деградирует и диспергирован: ±0.11), но преимущество RSFI там 0.7–3.5 п. — на грани статистической значимости. При N_ref ≥ 10 LogReg выигрывает всегда.
3. Практическая ценность ниши узка: сценарий «есть ровно 2–5 размеченных атак» редок (разметить 5 атак ненамного проще, чем 50).

**Финальная честная формулировка результата проекта** (которую можно защитить перед любым рецензентом):
> «Геометрический фильтр на SVD-подпространстве атак в whitened embedding-пространстве стабильно превосходит косинусный бейзлайн на ~4 п. AUC на 768d-эмбеддерах и конкурентоспособен с логистической регрессией в режиме ≤5 размеченных примеров. При ≥10 примерах и на 384d-эмбедерах преимущество исчезает».

Это workshop-уровень. Это честно. И это потолок.

---

## 6. Абляция: что именно даёт прирост (`honest_eval_v5.py`)

На обвинение «Kimi манипулирует: прирост дают ZCA и SVD, а не арккосинус» — экспериментальный ответ. Разложили пайплайн на компоненты (5 сидов, честный протокол):

```
===== all-mpnet-base-v2 =====      ===== BAAI/bge-base-en-v1.5 =====
  RAW_COS       : 0.7847             RAW_COS       : 0.7700
  WHITEN_ONLY   : 0.7750  (-1.0)     WHITEN_ONLY   : 0.7833  (+1.3)
  SVD_ONLY      : 0.8286  (+4.4)     SVD_ONLY      : 0.8256  (+5.6)
  FULL(ZCA+SVD) : 0.8207  (-0.8)     FULL(ZCA+SVD) : 0.8223  (-0.3)
```

**Выводы:**

1. **Прирост даёт SVD — и только SVD.** SVD_ONLY без всякого whitening — ЛУЧШИЙ вариант на обеих моделях. Обвинение в манипуляции несостоятельно: именно это аудит и признал во втором раунде (§1.1: «SVD-подпространство — не декорация»).
2. **ZCA-whitening — мёртвый груз.** В одиночку на mpnet вредит (−1.0 п.), в комбо вредит на обеих моделях (−0.8 / −0.3 п.). Убрав ZCA из «RSFI», метод становится лучше и проще. Брендовая часть проекта (ZCA + «римановость») — отрицательный вклад; рабочая часть — это PCA/SVD на эмбеддингах атак, т.е. «All-but-the-top» 2018 года.
3. Честный остаток новизны после абляции: «SVD-подпространство threat-эмбеддингов на сфере даёт +4…+6 п. AUC над косинусным центроидом на 768d-моделях». Без ZCA, без log-map, без τ, α, β.

---

## 7. Аудит `docs/math.md` (по просьбе автора: «проверь 2.5, её писала другая нейронка»)

**Разделы 2.1–2.4** — учебниковый материал, выписанный корректно: нормализация на сферу, геодезическое расстояние (3), ZCA-обеливание (5)–(7), касательное пространство (8), log-map (9) со свойством сохранения метрики (10), разложение на проекции (11)–(15), функционал RSFI (16) — всё соответствует коду `filter.py` и стандартной литературе. Математических ошибок нет; новизны тоже нет (это и признаёт автор: «половина формул — фундаментальные, как теорема Пифагора»).

**Раздел 2.5 (SVD/QR-подпространство)** — формулы (18)–(23) математически корректны (ортогональный проектор QQᵀ, идемпотентность, теорема Пифагора для ортогонального разложения). Ошибок в выкладках нет. Проблемы — не в формулах, а в приписках:

1. **Противоречие порогов:** в (17) заявлен «калиброванный на реальных данных τ* = +0.65», в (23) — «τ* = 0.0». Два несовместимых «калиброванных» порога в одном документе; ни один не выведен из валидационной процедуры.
2. **Финальная строка** («рост k=1..40 увеличивает покрытие до 93.7% и ROC-AUC до 0.8783 при задержке 22.1 мкс») — это числа из протокола с утечкой и k-on-test (§1.2: честная оценка 0.821), а «22.1 мкс» измеряет скоринг без стоимости эмбеддинга (§3, REVIEW.md).
3. Утверждение про защиту от «Evasion Attacks» эмпирически не проверялось (обфусцированные/адаптивные атаки в данных отсутствуют; по литературе — latent embedding collapse, arXiv:2605.19159 — там как раз отрицательный результат для таких методов).

**Вердикт по math.md:** математика — корректный пересказ учебника с правильными выкладками; «обосралась» нейронка не в формулах, а в численных заявлениях и противоречивых τ. Для статьи: убрать приписки, оставить вывод модели, добавить честный раздел экспериментов (§4–§6 настоящего документа).

---

## 8. Четвёртый раунд: высокоразмерный тест 4096d (запущен автором, `honest_eval_v6.py`)

По предложению автора метод проверен на **Qwen/Qwen3-Embedding-8B (4096d)** — размерность уровня скрытых слоёв полноразмерных LLM. Прогон выполнен автором на его GPU, честный протокол без изменений (результат: `high_dim_results.csv`):

```
===== Qwen/Qwen3-Embedding-8B (d=4096), 5 сидов =====
  RAW_COS        : 0.7974 ± 0.0078
  SVD_ONLY       : 0.8373 ± 0.0121
  FULL(ZCA+SVD)  : 0.8561 ± 0.0094
  LOGREG(ref)    : 0.8811 ± 0.0070
  (k по валидации: [10, 20, 20, 40, 10])
```

**Что это меняет — ревизия вердиктов аудита (честность в обе стороны):**

1. **РЕВИЗИЯ: ZCA-whitening на 4096d работает (+1.9 п. к SVD_ONLY).** Вердикт §6 «ZCA — мёртвый груз» справедлив для 384–768d, но в высокой размерности ZCA впервые даёт положительный и стабильный вклад (FULL > SVD_ONLY на всех 5 сидах). Аудит снимает формулировку «мёртвый груз» и заменяет её на: **вклад ZCA зависит от размерности — отрицательный на 384–768d, положительный на 4096d**.
2. **Прирост SVD над косинусом подтверждён и на 4096d** (+4.0 п., стабильно). Итого робастный факт: +3.6…+5.6 п. на всех моделях ≥768d.
3. **Лучший честный результат метода вырос до 0.8561** (4096d, полный пайплайн) — это уже близко к заявленным 0.8783, хотя всё ещё ниже их и получено без утечек.
4. **НЕ изменилось:** LogReg при равном бюджете по-прежнему лучше (0.8811, +2.5 п. над FULL). Закономерность «обученный классификатор > геометрический фильтр при N_ref=200» подтверждена на пятой модели и второй размерности.
5. Закономерность «больше d → лучше все методы» подтверждается: 384d (0.71–0.84) → 768d (0.77–0.87) → 4096d (0.80–0.88).

**Обновлённая честная формулировка результата проекта:**
> Геометрический фильтр (ZCA + SVD-подпространство угроз) даёт ROC-AUC 0.82–0.86 на эмбеддерах ≥768d (максимум на 4096d), стабильно превосходит косинусный бейзлайн на +4…+6 п., конкурентоспособен с LogReg при ≤5 примерах, но уступает ей при ≥10 примерах. Вклад ZCA положителен только в высокой размерности (4096d), на 384–768d он нейтрален или вреден.

---

*Все числа воспроизводимы: `python honest_eval_v2.py` … `honest_eval_v6.py`. Эмбеддинги закэшированы в `emb_cache/` — любая дополнительная модель проверяется за минуты.*
