name: CI RSFI Mathematical Benchmarks & Verification

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: src:.

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run RSFI Unit Test Suite
        run: python tests/run_all_tests.py

      - name: Run Advanced Mathematical Stress & Boundary Tests
        run: python tests/test_math_advanced.py

      - name: Run Legacy Test 1 (Basic Drift & Accuracy)
        run: python tests/legacy/test_ai.py

      - name: Run Legacy Test 2 (10-Stage ZCA vs PCA & Latency Benchmark)
        run: python tests/legacy/test2_aai.py

      - name: Run Legacy Test 3 (Dynamic Context Drift Simulation)
        run: python tests/legacy/test3_advanced.py

      - name: Run Legacy Test 4 (k-Subspace Threat Decoupling & Zero-Day Detection)
        run: python tests/legacy/test4_subspace.py
