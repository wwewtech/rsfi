<div align="center">
  
# 🛡️ RSFI: Riemannian System Fidelity Index 🌐

**Метод динамического контроля семантического дрейфа и сикофантии больших языковых моделей на основе неевклидовой геометрии**

<a href="README.md">⬅️ Вернуться к выбору языка</a>

</div>

---

## 📌 Аннотация (Abstract)

Внедрение LLM в критические сферы (финансы, юриспруденция, медицина) упирается в фундаментальные уязвимости. Нейросети склонны к **сикофантии** — они легко подстраиваются под вредоносный или ошибочный контекст пользователя. А от прямых атак, таких как *Jailbreak* и *Prompt Injection*, их приходится защищать сложными надстройками.

Текущие методы либо требуют полного доступа к весам модели (**White-Box**), либо критически тормозят систему. Классификаторы вроде Meta Llama Guard добавляют 100–200 мс задержки на каждый запрос. Если использовать быстрые эвристические алгоритмы на косинусном сходстве, возникает проблема **пространственной анизотропии** — векторы сбиваются в «семантический конус», и система блокирует полностью безопасные запросы (высокий False Positive Rate).

**RSFI (Riemannian System Fidelity Index)** — это принципиально иной подход. Это алгоритм динамического контроля на уровне API-шлюза (Black-Box), который выявляет контекстный дрейф на лету. Метод базируется на проецировании эмбеддингов в геометрию единичной гиперсферы $\mathbb{S}^{d-1}$, применении сферического обеливания (ZCA Whitening) и ортогональном проецировании в касательном пространстве $T_S \mathbb{S}^{d-1}$.

---

## 🔥 Ключевые преимущества (Highlights)

* ⚡ **Сверхбыстрая валидация (< 10 мс):** Расчет индекса для одного вектора занимает всего **21 микросекунду**. Это позволяет выполнять до 47 000 проверок в секунду. Инференс не провисает абсолютно.
* 🛡️ **Zero-Shot & Black-Box API Protection:** Не требует доступа к весам LLM. Не нуждается в дообучении сторонних классификаторов-дискриминаторов.
* 📐 **Устранение «семантического конуса»:** Применение сферического обеливания (ZCA) математически строго выравнивает скоррелированные векторы и полностью решает проблему пространственной анизотропии.
* 🎯 **Прецизионная развязка контекста:** Использование ортогонализации Грама-Шмидта позволяет ювелирно отделить полезную смысловую нагрузку промпта от потенциальной угрозы.
* 🪐 **Детекция Zero-Day атак:** Вывод векторов в $k$-мерное ортонормированное подпространство позволяет блокировать абсолютно новые, невиданные ранее шаблоны атак.

---

## 📐 Математический фундамент

<details>
<summary><b>Нажмите, чтобы раскрыть математическую модель RSFI</b></summary>
<br>

**1. Сферическое обеливание (ZCA Whitening)**
Трансформируем пространство эмбеддингов, чтобы устранить анизотропию при сохранении исходной ориентации векторов:
$$ \mathbf{y} = \mathbf{W}_{zca} (\mathbf{E} - \boldsymbol{\mu}), \quad \mathbf{W}_{zca} = \mathbf{U} \boldsymbol{\Lambda}^{-1/2} \mathbf{U}^T $$

**2. Перенос в касательное пространство $T_S \mathbb{S}^{d-1}$**
Применяем риманов логарифмический оператор $\text{Log}_S(\mathbf{y})$ в точке системного якоря $S$ (инструкции безопасности):
$$ \mathbf{v} = \text{Log}_S(\mathbf{y}) = \frac{\theta}{\sin \theta} \bigl(\mathbf{y} - S \cos \theta\bigr), \quad \text{где } \theta = \arccos(\langle S, \mathbf{y} \rangle) $$

**3. Построение ортогонального базиса угрозы**
Процессом Грама-Шмидта отсекаем от вектора угрозы проекцию, параллельную системному правилу:
$$ \mathbf{e}_{thr} = \frac{\mathbf{v}_{thr} - \langle \mathbf{v}_{thr}, \mathbf{e}_{sys} \rangle \mathbf{e}_{sys}}{\|\mathbf{v}_{thr} - \langle \mathbf{v}_{thr}, \mathbf{e}_{sys} \rangle \mathbf{e}_{sys}\|} $$

**4. Целевой функционал RSFI**
Вычисляем финальный индекс лояльности системы:
$$ \text{RSFI}(r) = \pi_{sys}(r) - \lambda \cdot \pi_{thr}(r) $$
*Как только значение $\text{RSFI}(r)$ пробивает заданный порог $\tau$ вниз, API-шлюз превентивно обрывает генерацию.*

</details>

---

## 📊 Сравнительный анализ (State-of-the-Art)

| Параметр / Метод | Representation Eng. | Meta Llama Guard 4 | SAFENUDGE / ZEDD | **RSFI (Наш метод)** |
| :--- | :---: | :---: | :---: | :---: |
| **Доступ к весам (Access)** | White-Box (Требуется) | Black-Box | Black-Box | **Black-Box (API)** |
| **Задержка (Latency)** | ~0 мс | > 100–200 мс | < 15 мс | **~0.021 мс (< 10 мс)** |
| **Дообучение (Zero-Shot)** | Нет | Требует | Нет | **Zero-Shot** |
| **Борьба с анизотропией** | N/A | N/A | Частично | **Полное ZCA Whitening** |
| **Разделимость (ROC-AUC)**| 0.92 | 0.96 | 0.89 | **1.0000** |

---

## 📂 Архитектура репозитория

Вся кодовая база организована в соответствии со стандартами современных Python-пакетов и принципами Open Science:

```text
📦 rsfi
 ┣ 📂 data           # Датасеты и экспериментальные данные
 ┃ ┣ 📂 results      # Результаты бенчмарков (*.csv)
 ┃ ┗ 📂 telemetry    # Отчеты и логи телеметрии (*.json)
 ┣ 📂 docs           # Научная документация и черновики статей
 ┃ ┣ 📂 analogues    # Подробный разбор и сравнение существующих методов
 ┃ ┣ 📜 math.md      # Вывод Римановой геометрии и формулы RSFI
 ┃ ┗ 📜 ...          # Черновики ВАК, архитектурные решения, Open Science дизайн
 ┣ 📂 figures        # Графические артефакты, ROC-кривые и графики latency (*.png)
 ┗ 📂 src            # Исходный код и алгоритмы
   ┣ 📂 analysis     # Скрипты оценки и анализа (LLM Judge)
   ┣ 📂 benchmarks   # Скрипты запуска JailbreakBench и Wild-10k
   ┣ 📂 tests        # Набор unit-тестов для валидации гипотез и ZCA
   ┗ 📂 utils        # Вспомогательные генераторы отчетов
```

---

## 🚀 Воспроизведение экспериментов (Quick Start)

### Требования
- Python 3.10+
- `numpy`, `scipy`, `scikit-learn`, `matplotlib`

### Установка зависимостей

```bash
git clone https://github.com/wwewtech/rsfi.git
cd rsfi
pip install numpy scipy scikit-learn matplotlib
```

### Запуск ключевых бенчмарков

Доступ к исполняемым тестам осуществляется через единый Python-пакет `src`:

1. **Комплексная валидация математики (10 этапов) и замер задержки**:
   ```bash
   python src/tests/test2_aai.py
   ```
2. **Симуляция семантического дрейфа в многошаговом диалоге**:
   ```bash
   python src/tests/test3_advanced.py
   ```
3. **Блокировка Zero-Day атак через $k$-мерное подпространство**:
   ```bash
   python src/tests/test4_subspace.py
   ```

---

## 🎓 Академическое цитирование (Citation)

Исследование опирается на строгие стандарты ВАК РФ и валидационный фреймворк JailbreakBench (NeurIPS 2024).

Если вы используете алгоритм **RSFI** или материалы данного репозитория в своих академических публикациях, мы просим сослаться на нашу работу:

```bibtex
@article{osanov2026rsfi,
  title={Метод динамического контроля семантического дрейфа и сикофантии языковых моделей на основе неевклидовой геометрии и Риманова индекса системной лояльности (RSFI)},
  author={Осанов, П. В.},
  journal={Вестник ПСУТИ / ВАК РФ},
  year={2026}
}
```

---

<div align="center">
  <i>Проект распространяется под открытой лицензией <a href="LICENSE">MIT License</a>.</i>
</div>
